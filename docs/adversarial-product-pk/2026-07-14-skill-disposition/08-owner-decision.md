# Owner Decision

- Date: 2026-07-14
- Decision: staged platform delivery (方案 A).
- Meaning: macOS is the first user-visible mutation release after its transaction gates pass. The native Windows adapter is the immediately following implementation batch and remains unsupported until its own native gates pass.
- Preserved constraints: Windows remains a priority; macOS evidence cannot be generalized to Windows; Git Bash is not mutation authority.
