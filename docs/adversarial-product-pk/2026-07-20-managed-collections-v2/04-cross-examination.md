# Cross-examination

| Question | Evidence-backed answer |
|---|---|
| Why is Better `prose-craft` not LangCraft `prose-craft`? | Repository, revision, source path, declared name and bytes differ; both nested entities are live. |
| May a Better receipt claim authorize deletion of a flat `prose-craft` path? | No. A basename-keyed receipt is a claim, not ownership. |
| What changes invalidate a v3 approval? | Any preserved collision path/target/health/claim change, source or controller change, scoped receipt change, predecessor change, or owned filesystem change. |
| Why are broken historical links not repaired? | The user did not select a projection owner or compatibility policy; auto-redirection would be an unauthorized semantic decision. |
| Why exclude `bs-visual-design` instead of quoting its description? | Silent byte rewriting would make the installed member diverge from the claimed immutable source. |
| Why retain the old 9-member generation? | Recovery/undo must interpret the exact historical plan; compatible profiles preserve that contract without promoting it as the current selection. |
| Why can `.DS_Store` remain? | It is exact portable host metadata, ignored only in deployed member/resource comparison; source/artifact/recovery and all other unknown entries remain strict. |
| Does fresh Codex discovery prove context reduction? | No. It proves this host exposes 64 members, not token savings or every Agent profile. |
