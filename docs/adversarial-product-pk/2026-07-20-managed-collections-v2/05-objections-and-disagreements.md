# Objections and Disagreements

## Resolved

| Objection | Resolution |
|---|---|
| same-name real directory blocks the other repository | preserve-by-default real-directory rule plus required-path exception and regression |
| collision is not immutable approval data | plan.v3 `preserved_collisions` and apply-time re-observation |
| broken targets hidden by READY | separate `name_collision_status` and `management_attention` |
| Better loader blocker | portable-YAML gate; current selection excludes `bs-visual-design`; no artifact patch |
| shared resources incomplete | four exact resources and closure from member/resource Markdown |
| nested Finder metadata causes false drift | recursive exact `.DS_Store` deployment exclusion; arbitrary-file drift regression |
| live LangCraft was still plan.v2, so foreign names were observed but not plan-bound | no-content plan.v3 generation `langcraft-1c6ef7cb054d`; exactly 10 foreign `prose-craft` preserves are now hashed and re-observed |
| predecessor-owned gateway exposures were mislabeled as preserved collisions | exclude exact predecessor exposure paths from collision observation; regression proves they remain transaction state, not foreign preserve state |

## Intentionally unresolved

1. Which repository, if any, should own each historical flat `prose-craft` projection. This requires explicit user disposition.
2. Runtime qualification for non-Codex Agent profiles.
3. Measured context-window reduction.
4. Append-only/tamper-evident catalog events and a shared cleanup/collection global lease.

These are explicit limitations, not hidden acceptance debt.
