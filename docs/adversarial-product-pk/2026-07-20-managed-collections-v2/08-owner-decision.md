# Owner Decision Record

The owner supplied the decisive correction:

> Better Skills repository's `prose-craft` and LangCraft repository's same-name Skill are two different things. A collision may be identified, but it must not authorize cleanup.

Implemented interpretation:

1. Skill identity is repository-qualified; basename is only a locator.
2. Foreign or unqualified same-name paths default to `preserve`.
3. No historical projection is deleted, redirected, adopted, or assigned a successor without a later explicit owner decision.
4. Broken target health must remain visible.
5. Content version and selected member profile are distinct: an immutable commit may contain a rejected member.

No decision was inferred for the future ownership of the 10 historical flat `prose-craft` projections.
