# 05 — Objections and Disagreements

## Resolved

- **O1:** candidate commits must be contained by local origin-tracking refs and materialized from exact Git objects. This does not
  become an online-remote claim.
- **O8:** current ProdCraft plan.v3 is strict; only exact historical plan.v1/v2 formats receive compatibility treatment. The live
  historical v1 generation no longer false-reports drift, and the successor generation is active.
- **Cross-repository deletion:** name equality is never replacement authority. Better and LangCraft identities remain separate.
- **Per-Skill lifecycle claim:** Panorama now keeps typed per-variant lifecycle evidence；complete provenance/lifecycle for every
  global identity remains outside the promotion scope.

## Retained limitations

- O3/O4/O5/O6/O9 remain bounded limitations, not hidden defects.
- Better's exact artifact is locally retained and digest-bound, but its revision object is not present in an unrelated current
  Better checkout; portable offline commit-to-file replay would require a retained Git bundle/object store.
- current-view collection operations have no append-only state journal; runtime profile does.
- whole active+physical collection loss is reported as recovery required and is not automatically rebuilt.
- active successor status does not traverse every historical predecessor repair quarantine; current repair/undo evidence is
  verified, while long-term bounded history audit remains P2.
- Panorama keeps collection `management_attention` in detailed rows but does not yet aggregate it in the top-level summary.

## Owner decisions

1. Confirm or reject the five exact active plan hashes in `10-promotion-boundary.md`.
2. Keep cleanup unapproved unless a separate exact identity-bound plan is reviewed.
3. Decide later whether cross-machine SLO, signed evidence or online remote attestation is worth the added system complexity.

Until decision 1 is recorded, the package verdict is `OWNER-DECISION-REQUIRED`, not final acceptance.
