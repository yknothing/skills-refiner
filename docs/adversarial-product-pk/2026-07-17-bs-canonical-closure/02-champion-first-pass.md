# Champion First Pass

**Visibility:** evidence map + completed P0/P1/P2-9/10 work  
**Evidence boundary:** filesystem + git remotes + scan JSON + unit tests  
**Not validated:** live model trigger rates for `bs-*`

## Strategy

Close the Better-Skills twin collision by making `bs-*` the only discoverable and authoring-present namespace on this machine, freeze the rule in POLICY, converge governance to `skill-hygiene`, and make inventory scans finish under a hard time budget.

## Why this is coherent

1. Collision was description-identical twins — discovery-side removal is the highest-leverage fix.
2. Authoring shelf prevents silent reappearance via sibling path resolution.
3. Control-plane doc prevents `loopos`/`grilling`/`skill-health` from stealing governance/SDLC defaults.
4. Scan bound prevents “governance tool that cannot run” regression.

## Claims that must not be made yet

- That every agent product on the machine will obey CONTROL-PLANE.md without product-level routing hooks.
- That `bs-skill-health` will never be confused with `skill-hygiene` by models.
