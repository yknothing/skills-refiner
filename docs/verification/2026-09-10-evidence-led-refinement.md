# skills-refiner 反模式诊断与归因边界完善

日期：2026-09-10。范围：在同日已验收、尚未提交的质量优先版本上继续完善源指导和评测，不以 Git HEAD 代替本轮实际基线。

## 改进范围

本轮吸收 [Anthropic 成本与性能文章](https://claude.com/blog/reducing-cost-and-improving-performance-with-claude-platform)及[配套审计规则](https://github.com/anthropics/skills/blob/main/skills/claude-api/shared/prompt-audit.md)中可迁移的方法，并保留其适用边界。没有引入厂商模型名称、价格、API 参数表或自动删除机制。

- 在已有 `conservative-evolution.md` 中明确：反模式信号用于定位待审内容，不能直接支持删除。按实际功能和执行条件判断，保留必要顺序、独立验证、罕见恢复及接收端输出契约；有证据的重复工作仍可精简。
- 把现有基线视为比较点，而非质量上限。用户要求提高质量时，不因达到旧及格线就转向压低成本。
- 分开内容改动与模型、effort、请求组装、缓存、价格和批处理的收益。认可配置组合中实际观察到的账单下降，但不自动归因于提示词压缩，也不改称 token 或质量收益。
- 补充跨工作负载的时限检查，并按 API 不兼容的真实来源区分 Skill 内容修复和应用代码修复。
- 同步 rubric，新增 11E/11F 两个合成校准快照及锚点，更新评测索引和 ADR 的局部进度。

五份 `SKILL.md` 入口均未修改；refiner 的 description 仍为 464 字符，部署和创建协作参考也保持本轮基线字节。11A–11D 的输入及锚点没有改动。未添加依赖、脚本、Skill、宿主配置或全局安装操作。[ADR-0009](../adr/0009-conservative-skill-evolution.md) 继续保持 `Proposed`。

## 独立源码评审

评审以本轮变更前工作区快照为基线，只审六份改动文件和入口路由。发现并修复一项 P2：初稿将所有 API 不兼容都归到请求构造层，可能漏掉 Skill 自身失效参数或示例的内容修复。最终规则改为先定位引入问题的 guidance 或 code，按各自作用范围和已有授权处理。独立复核确认此问题已解决，未发现修正引入的新问题。

## 行为验证

11E 同时检查必要规则保留和有依据的重复读取精简；11F 检查混合配置收益归因、真实账单观察、交互时限及质量缺口优先级。案例由本轮作者设计，不称为未知留出集。题目中的运行数据是给定证据，不是本轮测得的性能数据。

每版冻结入口与三个参考文件后，由独立执行者读取 Skill 和原始请求，先保存原始答复，再交匿名评审；执行者不读取锚点、其他版本或先前答复。每个执行者处理两个案例，批内共享上下文。模型和 effort 未显式覆盖，缺少可核对的 provider 路由和用量；这不是每例独立重复，也不是完整迁移或宿主工作流实测。

初稿与变更前版本的两例匿名比较均为实质相当，未发现关键或实质失败。源码评审修正后重新冻结最终包，另用新的执行者重跑两例；变更前答复继续冻结复用，初稿答复和评分保留。

最终由另一位独立匿名评审者比较冻结的变更前答复和最终答复，根任务再逐份复核：

| 案例 | 最终候选相对变更前版本 | 观察 |
|---|---|---|
| 11E：选择性反模式修订 | 实质相当 | 两版均保留恢复、顺序及载荷契约，允许精简重复状态读取，并单独处理应用兼容性问题。 |
| 11F：混合配置与质量优先 | 实质相当 | 两版均保留真实账单降幅，拒绝 token/质量及文本因果过度归因，识别交互时限，并优先补足权威裁决规则。 |

两轮匿名比较均未发现实质或关键失败。最终样本未出现本轮新增的行为回归，但两个单次校准案例不能证明普遍等效或整体质量提升。本轮完成的是指导明确化、源码缺陷修正和评测覆盖补充；没有测得 token、费用或耗时收益。修订后重新采样也不能证明答复差异由那三行修正引起。

## 静态与宿主证据

- Ruby Psych `3.1.0` 对仓库五份入口执行真实 YAML 解析；检查字段、名称、目录一致性及描述长度，全部通过。description 长度分别为 204、315、250、349、464 字符。
- 入口的三个本地引用存在且可读取；最终独立包的全部四份文件与当前源文件一致。
- 本机 `@google/gemini-cli` `0.39.1` 的原生 `loadSkillsFromDir` 已返回仓库五份入口及隔离包的名称、描述和正确正文。这是原生解析及正文读取证据，不是配置后的全局发现、触发、遵循或部署验收。
- 独立评审实际尝试 `codex debug prompt-input 'List available skills.'`，在 Skill 发现之前被 `/Users/whatsup/.codex/config.toml:20:1` 的 `invalid type: map, expected a boolean` 阻断；没有修改配置。此 Codex CLI 结果不覆盖 Gemini 解析器，也不代表所有宿主都无法加载。
- `skill-creator/scripts/quick_validate.py` 在系统 Python 与已有打包 Python 中均因缺少 `yaml` 模块无法启动。未安装新依赖；上述真实 YAML、字段、引用和原生解析检查独立完成，不将替代检查写成该脚本通过。

## 内容绑定与证据

本轮变更前 `conservative-evolution.md` 的 SHA-256 为 `d449bf721bb8e1645d7f5c58598b0c95d9ebf0d276e92409630fda0f66428011`，对应[上一轮验收](2026-09-10-quality-first-refinement.md)的资源快照。最终值为 `b1e292852b699f8cfeaab2b42789225f91a3cddab01a6bad57fbb8f9a99d527c`。上一轮报告和原始证据保持历史绑定，不把旧快照的检查转写成当前新内容的结果。

完整指导快照、两个输入、六份原始答复、加载记录、匿名映射、源码审查和检查结果存入[本轮证据](2026-09-10-evidence-led-refinement-evidence.json)。文本按 UTF-8 字节数和 SHA-256 绑定，临时路径只作为历史标识；复核不依赖临时目录继续存在。

完整包由 28,340 字符增至 30,557 字符，入口保持不变。这是静态字符计量，不是 token 或成本结果。增加内容用于补足具体判断边界，不将字数变化当成性能指标。

收尾核对通过：42 条记录对应的 31 段去重文本均通过 UTF-8 长度和摘要核对；最终四文件包、六份修改文件与归档一致；两轮匿名文件与原始答复映射一致；检查范围内 8 份 Markdown 的 21 个本地链接有效，`git diff --check` 通过。源仓库完善可以收口，运行收益和全局部署不在本轮完成声明内。

回退只需恢复本轮六份文件的变更前快照，并移除本轮报告和证据；应保留此前已接受的工作区改动及原有无关未跟踪内容。没有全局部署，因此不涉及安装回滚。
